// takes in argument of an object with form data(name and urls)

export const addDataSource = (dataSource) => {

    return dispatch => {
        dispatch({ type: 'START_ADDING_DATQA_SOURCE_REQUEST'})

        let configObj = {
            method: "POST", 
            headers: {
                ""
            }
        }
    }
}